{% set cardTags %}
	{% for tag in project.tags %}
		{{ tag.title | slug }}
		{% if not loop.last %},{% endif %}
	{% endfor %}
{% endset %}

<a href="/{{ site.navigation.archivePageSlug | slug }}/{{ project.slug | slug }}" 
class="projectsArchive-card" data-tags="{{ cardTags }}">
	<div class="content">
		<img src="{% imageUrlFor project.thumbnail %}" alt="{{ project.title }} Project Thumbnail" class="lazy"/>
		<div class="info">
			<h4>{{ project.title }}</h4>
			<p>{{ project.blurb }}</p>
		</div>
	</div>
</a>

